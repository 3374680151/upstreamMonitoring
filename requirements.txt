# Web 层：FastAPI/Uvicorn（ASGI）
fastapi>=0.128,<1
uvicorn>=0.39,<1
# 数据库：PyMySQL 驱动；连接池/生命周期复用 SQLAlchemy Engine（仅池化，不用 ORM）
PyMySQL>=1.1,<3
SQLAlchemy>=2.0,<3
# 后台定时调度：APScheduler（替代手写调度线程）
APScheduler>=3.10,<4
# 环境变量加载：python-dotenv（替代手写 .env 解析）
python-dotenv>=1.0,<2
# 上游 HTTP 客户端：httpx（连接复用/超时/重定向控制）
httpx>=0.28,<1
# TLS 指纹兼容传输：curl_cffi（连接被 Cloudflare 类上游重置时的回退通道，替代 subprocess curl）
curl_cffi>=0.13,<2
# 进程内 TTL 缓存容器：cachetools（有界内存 + 自动过期兜底）
cachetools>=5.3,<8
